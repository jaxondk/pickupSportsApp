const INITIAL_STATE = [];

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    // case SOME_ACTION_TYPE:
    //   return action.payload;
    default:
      return state;
  }
};
